# 智能代码仓库问答系统环境变量

# 服务器配置
HOST=127.0.0.1
PORT=8000
DEBUG=True

# GitHub配置（必需）
# 获取token: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here  # 这里必须是示例，不是真实token

# 模型配置
EMBEDDING_MODEL=BAAI/bge-small-zh
LLM_MODEL=gpt-3.5-turbo

# 数据库配置
CHROMA_PERSIST_DIR=./chroma_data